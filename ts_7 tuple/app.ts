// tuple is kind of array but more advance

let role:[string,string,number,number,boolean?]=["admin","manager", 1,3]  
role.push(true)
role[1]="user"

//tuple is similar where we define each element of array their kinds
//but in array we totally declare them either string, number or boolean